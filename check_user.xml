var user = engine.findDataByVarName("q_user");
var group = engine.findDataByVarName("q_group");

var groupId = group.getId();
var groupView = qgroupDao.findById(groupId);
var list = quserDao.findByQgroup(groupView);


engine.setDataByVarName("q_list",list);
